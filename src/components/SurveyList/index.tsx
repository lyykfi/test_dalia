import React, { FunctionComponent } from "react";

/**
 * A component with a list of survey.
 * @param props - A props for component.
 */
const SurveyList: FunctionComponent<{}> = (props) => {
  return (
    <div>test2</div>
  );
};

export default SurveyList;
