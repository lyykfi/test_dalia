import React, { FunctionComponent } from "react";

/**
 * A component with a list of survey.
 * @param props - A props for component.
 */
const SurveyListContainer: FunctionComponent<{}> = (props) => {
  return (
    <div>test2</div>
  );
};

export default SurveyListContainer;
